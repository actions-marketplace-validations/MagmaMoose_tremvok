# Chargate/KICS suppression (reviewed false positive, not a vulnerability):
#   baee238e... Passwords And Secrets - Generic Token misfires on the required
#   `id-token: write` GitHub OIDC permission key below — no secret is present.
# kics-scan disable=baee238e-1921-4801-9c3f-79ae1d7b2cbc
name: Release

# Diatreme release workflow (one image-aware template for the whole org).
#
# Detects a container build (docker-bake.hcl / Dockerfile):
#   - pull_request: image repos run `mode: ci` -> BUILD + push a pr-<N> image to
#     GHCR, so the build is verified BEFORE merge. Non-image repos no-op (the
#     required `diatreme` check passes trivially).
#   - push to the default branch: `mode: release` -> version + GitHub Release;
#     image repos also promote the image.
#
# versioning-tool defaults to gitversion (language-agnostic). Per-repo overrides:
#   - Python: versioning-tool: semantic-release-python (+ a pyproject config).
#   - Node:   versioning-tool: semantic-release-npm.
#   - Non-`main` default branch (e.g. master): set on.push.branches to match.
#
# All actions are SHA-pinned (Chargate's pre-commit hook enforces this); the
# trailing `# vX.Y.Z` comment tracks the tag each pinned SHA corresponds to.

on:
  pull_request:
  push:
    branches: [main]
    paths-ignore: ['**/*.md', 'docs/**', 'mkdocs.yml', 'LICENSE', '.gitignore']
  workflow_dispatch:

permissions:
  contents: write     # version bump + tag on release
  id-token: write     # OIDC for the Diatreme public-app token broker
  packages: write     # push the PR/release container image to GHCR

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  diatreme:
    # Same visibility-based routing as security.yml — see `template_vars` at the
    # top of settings.yml.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - id: detect
        shell: bash
        # image_name is set only when the repo builds a container image; Diatreme
        # gates all image steps on image_name != '', so non-image repos no-op.
        run: |
          if [ -f docker-bake.hcl ] || [ -f Dockerfile ]; then
            echo "image_name=${{ github.event.repository.name }}" >> "$GITHUB_OUTPUT"
          else
            echo "image_name=" >> "$GITHUB_OUTPUT"
          fi
      # PR: verify the image builds (image repos only). No `image-scan` here.
      #
      # Diatreme can scan the pr-<N> candidate, but that scan only feeds
      # `image-scan-gate` and reports to no sink by design. With the gate off
      # across the org it would be a Trivy run per pull request with no consumer,
      # so it stays off until we actually want to block on severity. The scan that
      # matters is on the release step below, against the image that ships.
      - if: github.event_name == 'pull_request' && steps.detect.outputs.image_name != ''
        uses: magmamoose/diatreme@415b19df7d00e776bf2b08bfe72823df8a1c6f23 # v2.16.0
        with:
          mode: ci
          image_name: ${{ steps.detect.outputs.image_name }}
      # Default-branch push (or manual dispatch): version + release.
      #
      # v2.16.0 IS THE FLOOR. It is the first release with the helm extras that
      # admin's `helm_chart_path` renders into this step (MagmaMoose/diatreme#194);
      # an older Diatreme warns "Unexpected input" for each of them and publishes
      # an unsigned, unlinted chart whose appVersion names an image tag nobody
      # pushed.
      # Three older fixes, each the difference between this working and looking
      # like it works:
      #   v2.13.0 (MagmaMoose/diatreme#177) — every image upload carries a
      #     `repo:<name>` tag. An image project is named after the IMAGE, and a
      #     repo may build several (dunmir -> dunmir-agent, -backend, -frontend),
      #     so nothing downstream can map a project back to its repo. Without the
      #     tag the DefectDojo sync gives dunmir FOUR products instead of one.
      #   v2.12.0 (MagmaMoose/diatreme#174) — the image scan runs in `mode: release`
      #     at all. Before it the scan was `mode: ci` only and the sinks were wired
      #     to the pr-<N> scan, so these inputs were accepted and did NOTHING.
      #   v2.12.1 (MagmaMoose/diatreme#176) — the BOM actually lands. Trivy emits
      #     CycloneDX 1.7, Dependency-Track 4.14.2 accepts up to 1.6 and rejects the
      #     rest with `HTTP 400 Unrecognized specVersion`. Every released image
      #     failed this way while the DefectDojo half of the same scan imported
      #     fine, leaving projects that exist and have never had a BOM.
      #
      # `image-scan` here scans the PROMOTED release tag and ships it: the
      # assembled image's CycloneDX SBOM to Dependency-Track and its Trivy
      # findings to DefectDojo. That is base-image packages plus whatever the
      # Dockerfile added — Chargate covers declared source dependencies only, so
      # without this a repo can sit in Dependency-Track looking covered while
      # nothing has ever inventoried the thing it actually ships.
      #
      # ON THE RELEASE STEP, not the PR step. The sinks describe what is DEPLOYED.
      # Reporting pr-<N> instead succeeds and looks correct, which is what makes it
      # the wrong default: Dependency-Track accumulates one project version per
      # pull request for ever, and the inventory ends up describing whichever PR
      # ran last rather than what is actually running.
      #
      # `image-scan` is REQUIRED — it defaults to false, so setting the sink URLs
      # alone does nothing. That is a second silent off-switch on top of the
      # "active iff the URL is set" one that kept Dependency-Track empty org-wide
      # for three months (#95); both have to be right or the job reports success
      # and ships nothing.
      #
      # NOT set, deliberately: `dependency-track-project-name` and
      # `-project-version`. Diatreme defaults the project to
      # `<image path> (image)` — distinct from Chargate's bare repo path, which is
      # what keeps the two SBOMs as two projects on one server — and the version to
      # the release tag. Pinning a name here would collapse them and each tool
      # would overwrite the other's inventory.
      #
      # DefectDojo gets the SAME product as Chargate with a different engagement
      # (Diatreme's default, "Diatreme image scan"): one product per repo, one
      # engagement per scan type, which is DefectDojo's own data model.
      - id: release
        if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
        uses: magmamoose/diatreme@415b19df7d00e776bf2b08bfe72823df8a1c6f23 # v2.16.0
        with:
          mode: release
          versioning-tool: gitversion
          environment: prod
          environments: '["prod"]'
          prerelease-identifiers: '{}'
          image_name: ${{ steps.detect.outputs.image_name }}
          image-scan: 'true'
          # DEPENDENCYTRACK / DEFECTDOJO, one word — check any edit against
          # `gh variable list --org MagmaMoose`. An undefined `vars.X` expands to
          # the empty string and both tools read that as "this sink is off".
          dependency-track-url: ${{ vars.MM_DEPENDENCYTRACK_URL }}
          dependency-track-api-key: ${{ secrets.MM_DEPENDENCYTRACK_API_KEY }}
          defectdojo-url: ${{ vars.MM_DEFECTDOJO_URL }}
          defectdojo-api-key: ${{ secrets.MM_DEFECTDOJO_API_KEY }}
          defectdojo-product-name: ${{ github.event.repository.name }}
          defectdojo-product-type: 'Research and Development'
      # Keep the floating major tag (v1, v2, ...) on the newest stable release, so
      # `uses: owner/repo@v2` means something. Nothing moved it before: chargate's
      # `v2` was set once and sat on v2.11.1 while the repo shipped v2.11.28, and
      # Chargate's own pin hook faithfully wrote `# v2.11.1` beside every SHA it
      # resolved from it. Diatreme moves its own major tag from a bespoke step in
      # its own release.yaml, which is why it is in `skip_repos` above.
      #
      # Scoped by `hashFiles` to repos that publish an action, because only those
      # are consumed by ref. Everything else keeps a clean tag list.
      #
      # Prereleases never move it: a floating major pointing at an rc would ship
      # one to every consumer pinned on the major. Pushed with the checkout's
      # GITHUB_TOKEN (`contents: write` above), which deliberately triggers no
      # workflows — a retag must not start another release.
      - name: Update floating major tag
        if: >-
          steps.release.outputs.released == 'true'
          && !contains(steps.release.outputs.version, '-')
          && hashFiles('action.yml', 'action.yaml') != ''
        env:
          RELEASE_TAG: ${{ steps.release.outputs.tag }}
          RELEASE_VERSION: ${{ steps.release.outputs.version }}
        run: |
          set -euo pipefail
          # `tag` carries whatever prefix the repo uses (v1.2.3, pkg-v1.2.3), so
          # recover it by stripping the version off the end rather than assuming `v`.
          prefix="${RELEASE_TAG%"${RELEASE_VERSION}"}"
          major_tag="${prefix}${RELEASE_VERSION%%.*}"
          # Fetch just the tag Diatreme pushed: the checkout is shallow, and
          # `--tags` on a shallow clone drags in far more than one ref needs.
          git fetch --force origin "refs/tags/${RELEASE_TAG}:refs/tags/${RELEASE_TAG}"
          target="$(git rev-list -n 1 "${RELEASE_TAG}")"
          # Lightweight on purpose — no annotation, so no committer identity and
          # nothing for a repo-level `tag.gpgSign` to try to sign.
          git tag -f "${major_tag}" "${target}"
          git push origin "refs/tags/${major_tag}" --force
          echo "${major_tag} -> ${target} (${RELEASE_TAG})"
